apply plugin: 'groovy'

dependencies {
  compile gradleApi()
  compile localGroovy()

  compile 'com.android.tools.build:gradle:1.2.3'
  compile 'com.squareup.wire:wire-compiler:1.8.0'
}

repositories {
  mavenCentral()
}
