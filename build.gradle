apply plugin: 'groovy'

dependencies {
  compile gradleApi()
  compile localGroovy()

  compile 'com.android.tools.build:gradle:1.0.0'
  compile 'com.squareup.wire:wire-compiler:1.8.0'
}

repositories {
  mavenCentral()
}
