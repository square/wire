buildCache {
  local {
    directory = new File(rootDir, '../.cache-include-paths-build-cache')
  }
}

include ":"

dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files('../../../../../gradle/libs.versions.toml'))
    }
  }
}
