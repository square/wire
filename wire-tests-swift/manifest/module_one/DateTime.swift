// Code generated by Wire protocol buffer compiler, do not edit.
// Source: squareup.protos.kotlin.swift_modules.module_one.DateTime in swift_module_one.proto
import Wire

/**
 * import "swift_module_address.proto";
 * import "swift_module_location.proto";
 */
public struct DateTime {

    /**
     * in usec; datum reference: the Unix epoch
     * What Joda calls instant except in usec rather than msec.
     *
     * Range: 1970 +/- 300K years
     */
    @ProtoDefaulted
    public var instant_usec: Int64?
    /**
     * in minutes; datum reference: UTC
     * What Joda calls offset except in minutes rather than msec.
     */
    @ProtoDefaulted
    public var timezone_offset_min: Int32?
    /**
     * as defined in draft-sharhalakis-httptz-05
     */
    @ProtoDefaulted
    public var posix_tz: String?
    /**
     * as defined in draft-sharhalakis-httptz-05
     */
    public var tz_name: [String] = []
    /**
     * An arbitrary value that can be used to break sorting ties when
     * instant_usec does not provide sufficient granularity (e.g.
     * System.nanoTime() in Java)
     */
    @ProtoDefaulted
    public var ordinal: Int64?
    public var unknownFields: UnknownFields = .init()

    public init(configure: (inout Self) -> Swift.Void = { _ in }) {
        configure(&self)
    }

}

#if !WIRE_REMOVE_EQUATABLE
extension DateTime : Equatable {
}
#endif

#if !WIRE_REMOVE_HASHABLE
extension DateTime : Hashable {
}
#endif

extension DateTime : Sendable {
}

extension DateTime : ProtoDefaultedValue {

    public static var defaultedValue: Self {
        .init()
    }
}

extension DateTime : ProtoMessage {

    public static func protoMessageTypeURL() -> String {
        return "type.googleapis.com/squareup.protos.kotlin.swift_modules.module_one.DateTime"
    }

}

extension DateTime : Proto2Codable {

    public init(from protoReader: ProtoReader) throws {
        var instant_usec: Int64? = nil
        var timezone_offset_min: Int32? = nil
        var posix_tz: String? = nil
        var tz_name: [String] = []
        var ordinal: Int64? = nil

        let token = try protoReader.beginMessage()
        while let tag = try protoReader.nextTag(token: token) {
            switch tag {
            case 1: instant_usec = try protoReader.decode(Int64.self, encoding: .variable)
            case 2: timezone_offset_min = try protoReader.decode(Int32.self, encoding: .signed)
            case 3: posix_tz = try protoReader.decode(String.self)
            case 4: try protoReader.decode(into: &tz_name)
            case 5: ordinal = try protoReader.decode(Int64.self, encoding: .variable)
            default: try protoReader.readUnknownField(tag: tag)
            }
        }
        self.unknownFields = try protoReader.endMessage(token: token)

        self._instant_usec.wrappedValue = instant_usec
        self._timezone_offset_min.wrappedValue = timezone_offset_min
        self._posix_tz.wrappedValue = posix_tz
        self.tz_name = tz_name
        self._ordinal.wrappedValue = ordinal
    }

    public func encode(to protoWriter: ProtoWriter) throws {
        try protoWriter.encode(tag: 1, value: self.instant_usec, encoding: .variable)
        try protoWriter.encode(tag: 2, value: self.timezone_offset_min, encoding: .signed)
        try protoWriter.encode(tag: 3, value: self.posix_tz)
        try protoWriter.encode(tag: 4, value: self.tz_name)
        try protoWriter.encode(tag: 5, value: self.ordinal, encoding: .variable)
        try protoWriter.writeUnknownFields(unknownFields)
    }

}

#if !WIRE_REMOVE_CODABLE
extension DateTime : Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringLiteralCodingKeys.self)
        self._instant_usec.wrappedValue = try container.decodeIfPresent(stringEncoded: Int64.self, firstOfKeys: "instantUsec", "instant_usec")
        self._timezone_offset_min.wrappedValue = try container.decodeIfPresent(Int32.self, firstOfKeys: "timezoneOffsetMin", "timezone_offset_min")
        self._posix_tz.wrappedValue = try container.decodeIfPresent(String.self, firstOfKeys: "posixTz", "posix_tz")
        self.tz_name = try container.decodeProtoArray(String.self, firstOfKeys: "tzName", "tz_name")
        self._ordinal.wrappedValue = try container.decodeIfPresent(stringEncoded: Int64.self, forKey: "ordinal")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringLiteralCodingKeys.self)
        let preferCamelCase = encoder.protoKeyNameEncodingStrategy == .camelCase
        let includeDefaults = encoder.protoDefaultValuesEncodingStrategy == .include

        try container.encodeIfPresent(stringEncoded: self.instant_usec, forKey: preferCamelCase ? "instantUsec" : "instant_usec")
        try container.encodeIfPresent(self.timezone_offset_min, forKey: preferCamelCase ? "timezoneOffsetMin" : "timezone_offset_min")
        try container.encodeIfPresent(self.posix_tz, forKey: preferCamelCase ? "posixTz" : "posix_tz")
        if includeDefaults || !self.tz_name.isEmpty {
            try container.encodeProtoArray(self.tz_name, forKey: preferCamelCase ? "tzName" : "tz_name")
        }
        try container.encodeIfPresent(stringEncoded: self.ordinal, forKey: "ordinal")
    }

}
#endif
