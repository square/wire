// Code generated by Wire protocol buffer compiler, do not edit.
// Source: squareup.protos.kotlin.oneof.OneOfMessageWithMessages in one_of.proto
import Foundation
import Wire

/**
 * A one of with message fields, to test merging of repeated occurrences on the wire.
 */
public struct OneOfMessageWithMessages {

    public var choice: OneOfMessageWithMessages.Choice?
    public var unknownFields: UnknownFields = .init()

    public init(configure: (inout Self) -> Swift.Void = { _ in }) {
        configure(&self)
    }

}

#if !WIRE_REMOVE_EQUATABLE
extension OneOfMessageWithMessages : Equatable {
}
#endif

#if !WIRE_REMOVE_HASHABLE
extension OneOfMessageWithMessages : Hashable {
}
#endif

extension OneOfMessageWithMessages : Sendable {
}

extension OneOfMessageWithMessages : ProtoDefaultedValue {

    public static var defaultedValue: Self {
        .init()
    }
}

extension OneOfMessageWithMessages : ProtoMessage {

    public static func protoMessageTypeURL() -> String {
        return "type.googleapis.com/squareup.protos.kotlin.oneof.OneOfMessageWithMessages"
    }

}

extension OneOfMessageWithMessages : Proto2Codable {

    public init(from protoReader: ProtoReader) throws {
        var choice: Choice? = nil
        var choiceProtoTag: UInt32? = nil
        var firstProtoData: Foundation.Data? = nil
        var secondProtoData: Foundation.Data? = nil

        let token = try protoReader.beginMessage()
        while let tag = try protoReader.nextTag(token: token) {
            switch tag {
            case 1: if choiceProtoTag != 1 { firstProtoData = nil }; choiceProtoTag = 1; try protoReader.decodeMessage(into: &firstProtoData)
            case 2: if choiceProtoTag != 2 { secondProtoData = nil }; choiceProtoTag = 2; try protoReader.decodeMessage(into: &secondProtoData)
            case 3: choiceProtoTag = 3; choice = .note(try protoReader.decode(String.self))
            default: try protoReader.readUnknownField(tag: tag)
            }
        }
        if choiceProtoTag == 1, let firstProtoData {
            choice = .first(try protoReader.decodeMergedMessage(OneOfMessageWithMessages.Inner.self, from: firstProtoData))
        }
        if choiceProtoTag == 2, let secondProtoData {
            choice = .second(try protoReader.decodeMergedMessage(OneOfMessageWithMessages.Inner.self, from: secondProtoData))
        }
        self.unknownFields = try protoReader.endMessage(token: token)

        self.choice = choice
    }

    public func encode(to protoWriter: ProtoWriter) throws {
        if let choice = self.choice {
            try choice.encode(to: protoWriter)
        }
        try protoWriter.writeUnknownFields(unknownFields)
    }

}

#if !WIRE_REMOVE_CODABLE
extension OneOfMessageWithMessages : Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringLiteralCodingKeys.self)
        if let first = try container.decodeIfPresent(OneOfMessageWithMessages.Inner.self, forKey: "first") {
            self.choice = .first(first)
        } else if let second = try container.decodeIfPresent(OneOfMessageWithMessages.Inner.self, forKey: "second") {
            self.choice = .second(second)
        } else if let note = try container.decodeIfPresent(String.self, forKey: "note") {
            self.choice = .note(note)
        } else {
            self.choice = nil
        }
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringLiteralCodingKeys.self)

        switch self.choice {
        case .first(let first): try container.encode(first, forKey: "first")
        case .second(let second): try container.encode(second, forKey: "second")
        case .note(let note): try container.encode(note, forKey: "note")
        case Optional.none: break
        }
    }

}
#endif

/**
 * Subtypes within OneOfMessageWithMessages
 */
extension OneOfMessageWithMessages {

    public enum Choice {

        case first(OneOfMessageWithMessages.Inner)
        case second(OneOfMessageWithMessages.Inner)
        case note(String)

        fileprivate func encode(to protoWriter: ProtoWriter) throws {
            switch self {
            case .first(let first): try protoWriter.encode(tag: 1, value: first)
            case .second(let second): try protoWriter.encode(tag: 2, value: second)
            case .note(let note): try protoWriter.encode(tag: 3, value: note)
            }
        }

    }

    public struct Inner {

        @ProtoDefaulted
        public var a: String?
        @ProtoDefaulted
        public var b: String?
        public var unknownFields: UnknownFields = .init()

        public init(configure: (inout Self) -> Swift.Void = { _ in }) {
            configure(&self)
        }

    }

}

#if !WIRE_REMOVE_EQUATABLE
extension OneOfMessageWithMessages.Choice : Equatable {
}
#endif

#if !WIRE_REMOVE_HASHABLE
extension OneOfMessageWithMessages.Choice : Hashable {
}
#endif

extension OneOfMessageWithMessages.Choice : Sendable {
}

#if !WIRE_REMOVE_EQUATABLE
extension OneOfMessageWithMessages.Inner : Equatable {
}
#endif

#if !WIRE_REMOVE_HASHABLE
extension OneOfMessageWithMessages.Inner : Hashable {
}
#endif

extension OneOfMessageWithMessages.Inner : Sendable {
}

extension OneOfMessageWithMessages.Inner : ProtoDefaultedValue {

    public static var defaultedValue: Self {
        .init()
    }
}

extension OneOfMessageWithMessages.Inner : ProtoMessage {

    public static func protoMessageTypeURL() -> String {
        return "type.googleapis.com/squareup.protos.kotlin.oneof.OneOfMessageWithMessages.Inner"
    }

}

extension OneOfMessageWithMessages.Inner : Proto2Codable {

    public init(from protoReader: ProtoReader) throws {
        var a: String? = nil
        var b: String? = nil

        let token = try protoReader.beginMessage()
        while let tag = try protoReader.nextTag(token: token) {
            switch tag {
            case 1: a = try protoReader.decode(String.self)
            case 2: b = try protoReader.decode(String.self)
            default: try protoReader.readUnknownField(tag: tag)
            }
        }
        self.unknownFields = try protoReader.endMessage(token: token)

        self._a.wrappedValue = a
        self._b.wrappedValue = b
    }

    public func encode(to protoWriter: ProtoWriter) throws {
        try protoWriter.encode(tag: 1, value: self.a)
        try protoWriter.encode(tag: 2, value: self.b)
        try protoWriter.writeUnknownFields(unknownFields)
    }

}

#if !WIRE_REMOVE_CODABLE
extension OneOfMessageWithMessages.Inner : Codable {

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: StringLiteralCodingKeys.self)
        self._a.wrappedValue = try container.decodeIfPresent(String.self, forKey: "a")
        self._b.wrappedValue = try container.decodeIfPresent(String.self, forKey: "b")
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: StringLiteralCodingKeys.self)

        try container.encodeIfPresent(self.a, forKey: "a")
        try container.encodeIfPresent(self.b, forKey: "b")
    }

}
#endif
